# Optional dependencies for FreeFlow.
# Install these only if you want the specific feature noted.
# The default install (requirements.txt) does NOT need any of these.

# quality_guard_level = "full" in settings.json enables cosine-similarity
# checking via a multilingual sentence-transformer model (~420 MB download
# on first use, ~10 ms CPU inference per call).
sentence-transformers>=2.7.0

# input_backend = "pynput" in settings.json enables the no-admin-rights
# hold-to-talk path (EXPERIMENTAL). Does not replace the keyboard library
# for the default path.
pynput>=1.7.0

# offline_fallback_enabled = true (default) in settings.json enables local
# transcription via faster-whisper when the Groq HTTP call fails with a
# network-class error (connection error, timeout, DNS). Optional, ~460 MB
# model download on first use.
faster-whisper>=1.0.0
